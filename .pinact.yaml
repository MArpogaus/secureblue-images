# yaml-language-server: $schema=https://raw.githubusercontent.com/suzuki-shunsuke/pinact/refs/heads/main/json-schema/pinact.json
# pinact - https://github.com/suzuki-shunsuke/pinact
version: 3

rules:
# The SLSA generator must be referenced by semver tag, not by commit SHA: the
# reusable workflow resolves its own source from the tag when it builds the
# provenance, and a SHA reference breaks verification.
- ignore: true
  conditions:
  - expr: |
      ActionName matches "slsa-framework/slsa-github-generator/.*" && ActionVersion matches "v\\d+\\.\\d+\\.\\d+"
